// Реалізуй функцію countOccurrences, яка приймає 2 аргументи phrase та part і повертає кількість разів
// що part з'являється у phrase.
// Рядки phrase і part містять виключно маленькі латинські літери. Літери в part не повторюються.
// countOccurrences("azyxxzyzy", "zy") === 3;
// countOccurrences("ababagalamaga", "bag") === 1;

function countOccurrences(phrase, part) {
  let counter = 0;
  let innerPhrase = phrase;
  let flag = true;
  console.log(innerPhrase)

  while(flag) {
    innerPhrase.findIndex
  }



}

console.log(countOccurrences("azyxxzyzy", "zy"));